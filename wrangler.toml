name = "katsu-katsu"
compatibility_date = "2026-03-26"

# Lets Cloudflare Pages / Wrangler know the static output lives in the repo root.
pages_build_output_dir = "."

# Explicit static assets config so `wrangler deploy` can publish this as a static site.
[assets]
directory = "."
